const set = require('./set');
const remove = require('./remove');
const push = require('./push');
const pushAll = require('./pushAll');
const pop = require('./pop');
const shift = require('./shift');
const unshift = require('./unshift');
const unshiftAll = require('./unshiftAll');
const insert = require('./insert');
const insertAll = require('./insertAll');

module.exports = { set, remove, push, pushAll, pop, shift, unshift, unshiftAll, insert, insertAll };
